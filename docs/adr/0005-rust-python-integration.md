# ADR 0005: Rust To Python Integration

## Status

Accepted pending toolchain.

## Decision

Use PyO3 and Maturin for Rust geometry bindings.

## Consequences

The current workstation is missing Rust, so only the crate skeleton and Python fallback tests are active.
